{
  "name": "opengym-mcp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "MCP (Model Context Protocol) bridge for openGym \u2014 read-only local stdio plus scoped remote HTTP catalogue/progress tools and reviewable routine proposals.",
  "license": "AGPL-3.0-or-later",
  "main": "src/index.js",
  "bin": {
    "opengym-mcp": "src/index.js"
  },
  "scripts": {
    "start": "node src/index.js",
    "start:http": "node src/http.js",
    "test": "vitest run",
    "check:node-loadable": "node scripts/check-node-loadable.mjs"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.4",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^4.1.11",
    "vitest": "^4.1.10"
  }
}
