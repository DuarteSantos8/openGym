{
  "name": "opengym-mcp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "MCP bridge for openGym workout data with optional paired-token plan creation over stdio; no extra container.",
  "license": "AGPL-3.0-or-later",
  "main": "src/index.js",
  "bin": {
    "opengym-mcp": "src/index.js"
  },
  "scripts": {
    "start": "node src/index.js",
    "test": "vitest run",
    "check:node-loadable": "node scripts/check-node-loadable.mjs"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.4",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^4.1.11",
    "vitest": "^4.1.10"
  }
}
